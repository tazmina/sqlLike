add User (name, age, location, company);
put (Alice, 10, NY, Google) to User;
put (Bob, 12, CA, Facebook) to User;
put (Cindy, 14, CA, Uber) to User;
put (David, 14, NY, Google) to User;
put (Alice, 14, CA, Google) to User;
get (_, 14, _, Google) from User;
set (_, _, _, Alphabet) to User (_, _, _, Google);
del (_, _, _, Alphabet) from User;
